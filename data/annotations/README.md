# Annotations Folder

Structure:

- `pilot_v0_4/`
  - `pilot_notes.md`: Pilot decisions and constraints.
  - `annotation_export_summary.md`: Export-level summary for the pilot set.
  - `cvat_export/annotations.xml`: CVAT XML export file for v0.4 pilot.

Notes:

- YOLO export placeholder was removed for this version.
- Add future formats as explicit sibling folders when activated.
