# IrisVisionAI — Global Path Contracts
# Version: v0.4 (LOCKED)
# Purpose: Single source of truth for all filesystem dependencies

# -------------------------------------------------------------------
# PRIVATE RESEARCH INFRASTRUCTURE (NOT PART OF GIT)
# -------------------------------------------------------------------

infra:
  root: /home/praneon/data/workspace/infra

datasets:
  casia_interval:
    root: ${infra.root}/image_splits/casia_interval
    images:
      pilot: ${datasets.casia_interval.root}/pilot
      train: ${datasets.casia_interval.root}/train
      val:   ${datasets.casia_interval.root}/val
      test:  ${datasets.casia_interval.root}/test

# -------------------------------------------------------------------
# FRAMEWORK RUNTIMES (PRIVATE, NOT VERSIONED)
# -------------------------------------------------------------------

nnunet:
  root: ${infra.root}/frameworks/nnunet
  raw: ${nnunet.root}/raw
  preprocessed: ${nnunet.root}/preprocessed
  results: ${nnunet.root}/results

# -------------------------------------------------------------------
# PROJECT REPOSITORY (VERSIONED, PUBLIC)
# -------------------------------------------------------------------

project:
  repo_root: /home/praneon/data/workspace/projects/IrisVisionAI

  annotations:
    root: ${project.repo_root}/data/annotations

  metadata:
    root: ${project.repo_root}/data/metadata
    csv: ${project.metadata.root}/metadata.csv

  splits:
    root: ${project.repo_root}/data/splits

  working:
    root: ${project.repo_root}/data/working  # temporary artifacts only

  outputs:
    root: ${project.repo_root}/outputs

